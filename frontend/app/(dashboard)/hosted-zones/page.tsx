import { HostedZonesPage } from "@/features/hosted-zones/hosted-zones-page";

export const metadata = {
  title: "Hosted Zones — Route 53 — AWS Management Console",
};

export default function HostedZones() {
  return <HostedZonesPage />;
}
